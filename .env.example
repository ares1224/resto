# Variables d'environnement — copier en .env.local pour le développement,
# et reporter les valeurs dans Vercel > Settings > Environment Variables.
#
# En l'état, l'application démarre sans aucune variable : les données vivent
# dans le fichier data/restaurant.json créé au premier lancement.
#
# Compte super-admin (propriétaire de la plateforme). Créé uniquement s'il
# n'existe pas encore. Changez le mot de passe dès la première connexion.
SUPERADMIN_EMAIL=admin@platform.local
SUPERADMIN_PASSWORD=ChangeMe123!

# URL publique de l'application (liens dans les emails de confirmation).
# APP_URL=http://localhost:3000

# Envoi d'emails (optionnel). Sans clé, le lien de confirmation est affiché
# sur la page d'inscription après soumission.
# RESEND_API_KEY=
# RESEND_FROM=Bistrot Gestion <noreply@resend.dev>

# Stockage persistant pour l'hébergement (voir README, déploiement Vercel).
# DATABASE_URL=
# BLOB_READ_WRITE_TOKEN=
